# Production character

This directory contains normalized working copies used by the editor. Originals remain untouched in the top-level archive.

The Phase 1 character is `algowzxd-character.svg`, copied from the segmented full-body SVG identified in `CHARACTER_ASSET_AUDIT.md`.
